number = int(input("Enter a number: "))# feed number as after running the code.

if number%2 == 0: # if the number is even it will print to even else return to esle statement.
    print("Even")
else:
    print("Odd")

"""Input: The program uses input() to take user input
and int() to convert it to an integer number.

Condition Check: The if statement checks that the number
is divisible by 2 that is (number % 2 == 0). If true, the number is even; otherwise,return to else condition and it is odd.

Output: The program prints "Even" or "Odd"."""
